# 2026-06-28 小程序端

## Phase 3 完成：小程序核心功能完整并提交备案

### 1. UI 重构
- 入口页（首页）改为深蓝灰渐变风格，标题改为「江苏公考AI智能训练平台」
- 面试页卡片改为饱和渐变色，与 Web 端一致
- 申论页优化入口设计，减少下方空白
- 要闻页支持复制链接并提示「粘贴到浏览器打开」
- 「我的」页面采用深灰渐变头部，统一字体规范（标题16-20px，正文14px，辅助12px）

### 2. 语音答题
- 接入阿里云 ASR 小程序 SDK（`src/utils/aliyun-nls/`）
- 封装 `VoiceInput` 组件，支持面试真题与申论作答区
- 实现「按住说话/松开结束」微信风格交互
- 后端 `/api/voice/aliyun-token` 获取阿里云临时 Token，进程内缓存

### 3. 申论训练模块完善
- 详情页「作答」Tab 默认展开作答区
- 接入 OCR 手写识别与 AI 批改
- 题型筛选标签统一为 6 个标准分类（归纳概括/综合分析/提出对策/贯彻执行/公文写作/大作文/经验总结）

### 4. 项目与备案
- 本地代码推送到 GitHub：`1187220471/jiangsu-gongkao-ai-miniapp`
- 小程序已提交备案

## 提交记录
- 推送 commit `d05fd9e` 到 `1187220471/jiangsu-gongkao-ai-miniapp`，包含 27 个文件变更（UI 重构、语音、申论、记忆更新）

## 踩坑记录
- 组件 SCSS 不生效 → Taro 对 `src/components/**/*.scss` 不编译，改用内联 style
- 语音 SDK 路径错误 → 修正相对路径到 `src/utils/aliyun-nls/`
- `process is not defined` → 小程序无 `process.env`，Token 从后端获取
- 阿里云 Token 接口 500 → 改 HTTPS + 重新创建 AccessKey + 后端缓存
- wss 域名未配置 → 微信公众平台添加 `wss://nls-gateway.cn-shanghai.aliyuncs.com`
- 模拟器 IDLE_TIMEOUT → 真机测试，先启动录音再启动 ASR，加录音锁
- 识别结果未回填 → 用 `interimTextRef` 兜底

## 待办
- 等待备案/认证审核
- 配置最终服务器域名
- 上传代码提交审核
